# Ground Truth

Manually-verified SQL results from `data/orders.db` (seeded by `app/db_setup.py`).
Every agent answer must match these — this is the proof behind "grounded execution."

Generated by running each query directly against SQLite (Python `sqlite3` stdlib, same engine as the CLI).

---

## 1. Total revenue by region

```sql
SELECT region, SUM(quantity * price) AS revenue FROM orders GROUP BY region;
```

| region | revenue |
|---|---|
| East | 8000.0 |
| North | 168500.0 |
| South | 56500.0 |
| West | 16000.0 |

## 2. Orders in March 2026

```sql
SELECT * FROM orders WHERE order_date LIKE '2026-03%';
```

| id | customer_name | product | quantity | price | order_date | region |
|---|---|---|---|---|---|---|
| 4 | Sneha Iyer | Laptop | 1 | 55000.0 | 2026-03-10 | South |
| 5 | Aman Gupta | Keyboard | 5 | 700.0 | 2026-03-18 | North |
| 6 | Priya Nair | Monitor | 1 | 8000.0 | 2026-03-22 | East |

**Count of March 2026 orders: 3**

## 3. What Ravi Kumar ordered

```sql
SELECT product, quantity FROM orders WHERE customer_name = 'Ravi Kumar';
```

| product | quantity |
|---|---|
| Laptop | 1 |

## 4. Total revenue from the North region

```sql
SELECT SUM(quantity * price) FROM orders WHERE region = 'North';
```

**168500.0** (= 1×55000 + 5×700 + 2×55000)

## 5. Best-selling product by quantity

```sql
SELECT product, SUM(quantity) AS total_qty FROM orders GROUP BY product ORDER BY total_qty DESC LIMIT 1;
```

**Keyboard — 5 units** (Laptop 4, Mouse 3, Monitor 3)

## 6. Total row count

```sql
SELECT COUNT(*) FROM orders;
```

**7**

---

## Demo questions to ask the agent (expected grounded answers)

| Question | Expected answer contains |
|---|---|
| What is the total revenue from the North region? | 168500 |
| What products did Ravi Kumar order? | Laptop (quantity 1) |
| How many orders were placed in March 2026? | 3 |
| Which region had the highest revenue? | North (168500) |
| What is the best-selling product by quantity? | Keyboard (5) |
| What's the capital of France? | Refusal — out of scope |
